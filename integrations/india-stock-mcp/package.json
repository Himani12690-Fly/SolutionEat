{
  "name": "india-stock-mcp-http",
  "private": true,
  "version": "1.0.0",
  "description": "Runs india-stock-mcp (NSE/BSE quotes, options chain, fundamentals) as a remote Streamable HTTP MCP server via supergateway, for use as a Claude custom connector.",
  "type": "module",
  "scripts": {
    "postinstall": "patch-package",
    "start": "supergateway --stdio \"node node_modules/india-stock-mcp/dist/index.js\" --outputTransport streamableHttp --port $PORT --stateful --sessionTimeout 1800000"
  },
  "dependencies": {
    "india-stock-mcp": "^1.0.1",
    "supergateway": "^3.4.3",
    "patch-package": "^8.0.1"
  }
}
